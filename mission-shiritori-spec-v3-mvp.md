# ミッションしりとり Webアプリケーション仕様書 v3（MVPスコープ版）

- 仮タイトル: **ミッションしりとり**
- 文書種別: 企画・ゲームルール・システム設計仕様書
- 想定用途: 副業応募時のポートフォリオ
- 想定開発者数: 1人
- 想定開発期間: 1〜2週間（副業・本業と並行）
- v2からの変更方針: 遊びの厚みより完成を優先し、技術的な見せ場を残したまま機能を絞った

---

## 0. v2からの変更点

v2は仕様として破綻していないが、個人が1〜2週間で完成させる規模を大きく超えていた。
以下の基準で機能を削った。

**残した基準**：セクション2.2の「ポートフォリオ目標」に挙げた技術要素を示すのに必要なもの
**削った基準**：ゲームとしての厚み・バリエーションを増やすだけで、技術的な見せ場を増やさないもの

| 項目 | v2 | v3 |
|---|---|---|
| モード | スコアアタック／デイリー／ステージの3種 | デイリーチャレンジ1本 |
| ミッション種別 | 9カテゴリ・数十パターン | 4種類（文字数・カテゴリ・末尾文字・単語数） |
| コンボ倍率 | あり | なし（v2で見せたい効果は同時達成ボーナスで代替） |
| ゴースト比較 | あり | 将来候補へ |
| 取り消し機能 | あり | 将来候補へ（ヒントのみ残す） |
| ランキング表示 | パーセンタイル・中央値・代表ルート等 | 順位・自己ベストのみ |

削ったものは「実装しない」であって「考えていない」ではない。第16章に将来候補として残す。
これにより、README・面談で「スコープを意図的に絞った」ことを判断として説明できる。

---

## 1. 企画概要

### 1.1 コンセプト

一般的なしりとりは語彙力と回答速度にゲーム性が偏る。本アプリでは、プレイ開始時に複数の**ミッションカード**が配られ、プレイヤーは制限時間内にしりとりをしながら、カードの条件を達成して得点を稼ぐ。

例:

- 動物カテゴリの単語を2回使う
- 5文字以上の単語を2回使う
- 「る」で終わる単語を使う

単に長く続けるのではなく、「どの単語なら複数のミッションを同時に進められるか」「次にどの文字へ進めば残りのミッションを達成しやすいか」を考えるルート構築パズルとする。

### 1.2 企画上の強み（MVPでも有効なもの）

- 一人で完結するため同時接続ユーザーが不要
- WebSocketを使わずに成立する
- 自由チャット・画像投稿・ユーザー間の直接連絡が不要
- 日本語正規化、辞書検索、ルールエンジンなど技術的な見せ場がある
- ミッションをデータ駆動で追加でき、拡張性を示せる

---

## 2. 目標

### 2.1 プロダクト目標（MVP）

1. 初見のユーザーが説明を読まなくても30秒以内に遊び始められる
2. 1プレイを60〜120秒程度で完結させる
3. 毎日1回遊びたくなるデイリーチャレンジを提供する

### 2.2 ポートフォリオ目標

採用担当者や副業の発注者に、以下を示す。

- React / Next.js による Web UI 開発
- TypeScript による型安全な実装
- REST API 設計
- PostgreSQL を使ったデータ設計
- 日本語文字列の正規化
- 検索性能を意識した辞書設計
- JSON ベースのルールエンジン（ミッション判定）
- サーバー側でのスコア計算・不正対策
- 自動テスト
- CI/CD
- クラウドデプロイ
- 「なぜこの構成にしたか」を説明できる設計判断（第14章）

これらはモードの数やミッションの種類を増やしても増えない。**MVPの範囲で全て示せる**。

---

## 3. 想定ユーザー

### 3.1 メインユーザー

- しりとりや言葉遊びが好きな人
- スマートフォンで短時間遊びたい人
- 毎日のスコアアタックが好きな人

### 3.2 対象外（初期版）

- 自由チャット
- 画像・動画・音声のアップロード
- ユーザーによる自由な問題投稿
- リアルタイム対戦
- 賞金・換金要素

---

## 4. ゲーム構成（MVP：デイリーチャレンジのみ）

全ユーザーがその日ごとに同じ条件で挑戦する、単一モードのみを実装する。

- 同じ開始単語
- 同じミッション候補（3枚）
- 同じ制限時間（60秒）
- 同じルールセット
- 同じランダムシード
- ランキング対象は1日最初の完走記録（再挑戦は「練習」として可能）

スコアアタック（毎回ランダム）とステージモード（チュートリアル的な段階学習）は実装せず、第16章の将来候補とする。デイリーチャレンジ1本でも、辞書・ルールエンジン・スコア計算・ランキングAPIという主要な技術要素は全て必要になるため、ポートフォリオとしての説明力は落ちない。

---

## 5. 基本ゲームルール

### 5.1 プレイ開始時に決定する値

- 開始単語
- 制限時間（60秒固定）
- 使用する辞書バージョン
- ミッションカード（3枚）
- ランダムシード
- ランキング対象かどうか

### 5.2 単語入力の判定順序

サーバー側で以下を判定する。

1. 入力形式が正しいか
2. 辞書に登録されているか
3. 許可された品詞か
4. 前の単語の末尾と接続しているか
5. すでに同じプレイ内で使用されていないか
6. 「ん」で終わっていないか
7. ゲーム時間内の送信か
8. リクエストが改ざんされていないか

### 5.3 成功時の処理

- ルートに単語を追加
- 基本点を加算
- 各ミッションの進捗を更新
- ミッション達成を判定・報酬を適用
- クライアントへ最新状態を返す

### 5.4 失敗時の処理

| エラー | 処理 |
|---|---|
| 辞書にない／接続不成立／使用済み単語 | 入力を不採用。短いペナルティ（時間-2秒） |
| 「ん」で終了 | 得点加算後にゲーム終了 |
| 時間切れ | プレイ終了 |
| 通信失敗 | 再送可能。サーバーの結果を正とする |

### 5.5 終了条件

- 制限時間が0になる
- 「ん」で終わる単語を確定する
- プレイヤーが終了ボタンを押す
- 一定時間以上通信が切断される

---

## 6. しりとりの文字ルール

日本語の表記揺れは不公平・不具合の原因になるため明文化する（この章はv2から変更なし。日本語正規化はポートフォリオ上の主要な見せ場のため維持する）。

### 6.1 入力形式

- ひらがな入力を推奨、カタカナは自動的にひらがなへ変換
- 全角・半角は正規化、前後の空白は除去、内部空白・記号は原則禁止
- Unicode正規化はNFKCを使用

### 6.2 濁音・半濁音

- 「か」と「が」、「は」「ば」「ぱ」は別の開始文字として扱う
- 接続時は完全一致を原則とする

### 6.3 小書き文字

単語末尾が小書き文字の場合、通常サイズの文字として扱う。

- ぁぃぅぇぉ → あいうえお／ゃゅょ → やゆよ／っ → つ／ゎ → わ

### 6.4 長音記号

カタカナ語の末尾が「ー」の場合、直前の音節の母音として扱う（例: コーヒー→い）。実装の曖昧さを避けるため、辞書側に `lastKana` をあらかじめ保存する。

### 6.5 「ん」

正規化後の末尾が「ん」の単語を確定した場合、得点加算後にゲーム終了とする。

### 6.6 同音異義語

読みが完全に同じ単語（橋／箸／端）は、初期版では同一単語として扱い、同一プレイ内で1回のみ使用可とする。

### 6.7 品詞

初期版では一般名詞・サ変接続名詞を中心とし、動詞の活用形・形容詞・助詞・助動詞・記号・人名・企業名・スラング・不適切語は禁止とする。

---

## 7. 単語辞書

### 7.1 辞書レコード

```json
{
  "id": "word_01H...",
  "surface": "ゴリラ",
  "reading": "ごりら",
  "normalized": "ごりら",
  "firstKana": "ご",
  "lastKana": "ら",
  "characterCount": 3,
  "partOfSpeech": "noun",
  "isProperNoun": false,
  "isAllowed": true,
  "categories": ["animal"],
  "dictionaryVersion": 1
}
```

v2にあった `moraCount`、`difficulty`、`tags` はMVPでは未使用のため削除した。使わないフィールドを持たせると、辞書生成スクリプトとテストの両方に無駄な複雑さが増える。必要になったらマイグレーションで追加すればよい。

### 7.2 カテゴリ（MVPでは5種類に限定）

- animal（動物）
- food（食べ物）
- plant（植物）
- tool（道具）
- place（場所）

v2の10種類から5種類に削減。ミッション種別を絞ったこととも整合する。

### 7.3 辞書データの作成方針

1. オープンライセンスの形態素辞書を基礎データにする
2. 品詞と固有名詞を機械的に絞り込む
3. 不適切語を除外する
4. カテゴリは自動分類＋手動確認
5. 一般名詞を中心に1万〜3万語程度に抑える（v2の3万〜10万語から縮小。MVPの学習・確認コストを優先）

初期版ではユーザーからの単語投稿機能は実装しない。

---

## 8. ミッションカード（4種類に限定）

### 8.1 目的

- プレイごとに異なる最適ルートを作る
- 語彙力だけでなく戦略性を生む
- スコアの主な獲得源とする

### 8.2 カード構成

```json
{
  "id": "mission_animal_2",
  "name": "動物博士",
  "description": "動物カテゴリの単語を2回使う",
  "baseScore": 300,
  "type": "category_count",
  "params": { "category": "animal", "target": 2 },
  "reward": { "type": "add_time", "value": 3 },
  "enabled": true
}
```

### 8.3 ミッション種別（MVPで実装する4種類のみ）

| 種別 | 例 |
|---|---|
| 単語数系 | 5語つなぐ／10語つなぐ |
| 文字数系 | 5文字以上の単語を2回使う |
| カテゴリ系 | 動物を2回使う／食べ物を3回使う |
| 末尾文字系 | 「る」で終わる単語を使う |

v2にあった開始文字系・速度系・連続順序系・制約系・高難度系は削除し、第16章の将来候補とする。判定ロジック（`type` に応じたルールエンジンの分岐）という技術的な見せ場は4種類でも十分に示せる。むしろ種類を絞ることで、各判定ロジックのテストを丁寧に書く余裕ができる。

### 8.4 ミッションの組み合わせ制御

完全ランダムでは同時達成不能な組み合わせが発生するため、以下は維持する（これ自体がアルゴリズム設計力の見せ場になるため）。

- ミッションごとにタグを付ける
- 相性の悪いタグの同時出現を禁止する
- 開始単語と辞書分布から達成可能性を推定する

v2にあった「難易度合計の上限・下限」「同種ミッションの重複抑制」の細かい調整ロジックは、まず単純な組み合わせ禁止リストのみで実装し、必要になったら拡張する。

### 8.5 ミッション達成報酬（MVPでは1種類のみ）

- スコア加算のみ

v2にあった時間延長・得点倍率・ペナルティ無効・カード交換は将来候補とする。報酬が1種類でも、ミッション達成の判定・進捗更新・スコア加算というAPIの流れは変わらない。

---

## 9. スコア設計（コンボなし）

### 9.1 基本方針

- 得点の中心はミッション達成
- スコア計算はサーバー側のみで行う
- スコア計算式にバージョンを付ける

### 9.2 基本スコア

| 条件 | 得点 |
|---|---:|
| 有効な単語を追加 | 100 |
| 5文字以上 | +20 |
| ミッション達成 | カード定義の値 |
| 2枚同時達成 | +100 |
| 3枚同時達成 | +300 |
| 無効入力 | -2秒 |
| 「ん」で終了 | その時点で終了 |

v2にあった「初めて通る末尾文字」「初めて使うカテゴリ」「回答速度ボーナス」「コンボ倍率」は削除した。理由は、これらが加わるとスコア計算式の項が増え、テストケースの組み合わせが増えるわりに、見せたい技術（サーバー側計算・不正対策・バージョニング）は増えないため。

### 9.3 スコア計算式

```text
moveScore = baseWordScore + lengthBonus

totalScore =
  sum(moveScore)
  + missionScore
  + simultaneousCompletionBonus
```

### 9.4 終了ボーナス

- 全ミッション達成: +500
- ミス0回: +300

v2の「10種類以上の末尾文字」ボーナスは末尾文字系ミッションと重複するため削除。

### 9.5 スコアバージョン

ランキングの公平性のため、以下は必ず保存する（MVPでも省略しない）。

- scoring_version
- dictionary_version
- mission_rule_version

---

## 10. 補助機能（ヒントのみ）

### 10.1 ヒント

次に使用可能な開始文字から始まる単語数を表示する（具体的な単語は表示しない）。1プレイ1回まで。

v2にあった「取り消し機能」は削除した。取り消しは直前の得点・進捗を巻き戻す処理が必要で、実装コストの割に見せられる技術要素が増えないため、将来候補とする。

---

## 11. デイリーチャレンジ生成

v2の設計をそのまま採用する（すでに個人開発の規模として妥当だったため変更なし）。

### 11.1 生成方法

```text
seed = HMAC_SHA256(secret, "2026-07-28:daily:v1")
```

シードから開始単語・ミッション候補・制限時間を決定する。

### 11.2 公平性

- 同じ日・同じルールバージョンでは全員同条件
- 日付境界はAsia/Tokyo
- デイリー条件はサーバーで保存する

### 11.3 挑戦回数

- 何度でも挑戦可能。公式ランキングには最初の完走記録を採用し、2回目以降は「練習」と明示する

### 11.4 結果表示（MVPでは最小限）

- 自分の得点
- 順位
- 自己ベストとの差

v2にあった「パーセンタイル」「全参加者平均」「中央値」「代表ルート表示」は、ランキング参加者が少ない初期段階では意味を持ちにくいため将来候補とする。

---

## 12. API設計

```text
POST /api/v1/game-sessions
POST /api/v1/game-sessions/:id/moves
POST /api/v1/game-sessions/:id/finish
GET  /api/v1/rankings/daily
```

v2にあった `/replay`（リプレイ）エンドポイントは、イベントログの保存自体は行うが、専用の閲覧APIはMVPでは作らない。ログさえ残しておけば後から追加できる。

---

## 13. データモデル（最小限）

MVPで必要なテーブルは以下のみ。

- `words`（辞書）
- `missions`（ミッション定義）
- `game_sessions`（プレイセッション：開始単語・ミッション・状態・スコア）
- `game_moves`（入力イベントログ：単語・判定結果・タイムスタンプ）
- `daily_challenges`（日付ごとの条件）

v2で言及されていた詳細な進捗テーブルの正規化は、`game_sessions` にJSONBカラムでミッション進捗を持たせることで簡略化してよい。正規化のしすぎは個人開発の速度を落とす。

最低限のインデックス:

```sql
CREATE UNIQUE INDEX words_normalized_version_unique
ON words(normalized, dictionary_version);

CREATE INDEX game_sessions_daily_score_idx
ON game_sessions(daily_date, score DESC)
WHERE game_mode = 'daily' AND is_ranked = true AND status = 'finished';
```

---

## 14. 技術スタック・運用方針（v2第47章を踏襲）

この章はv2の中で最も完成度が高く、ポートフォリオとしての説明力も強いため、要点のみ引き継ぐ。

### 14.1 構成

```text
[Browser] → HTTPS → [Vercel: Next.js (UI + Route Handlers)] → [Neon PostgreSQL]
```

| 役割 | サービス | 費用 |
|---|---|---:|
| Web・API | Vercel Hobby | 0円 |
| DB | Neon Free | 0円 |
| CI | GitHub Actions | 0円 |
| 定期処理 | Vercel Cron + アクセス時遅延生成 | 0円 |
| 監視 | Sentry Free | 0円 |

### 14.2 FastAPI・Redisを使わない理由

- MVP規模の処理はNext.js Route HandlersとPostgreSQLで完結する
- 別サービスに分けるとCORS、環境変数共有、認証連携、コールドスタート対策など、機能を増やさない運用コストだけが増える
- ただし将来分離できるよう、ゲームロジックを `domain` / `application` / `infrastructure` に分離し、Reactコンポーネントに書かない

### 14.3 移行条件（あらかじめ決めておく）

FastAPI分離: Pythonの形態素解析処理をオンラインAPIで使う必要が出た場合、または複数クライアントから利用する必要が出た場合。
Redis追加: ランキングAPIのDB負荷が実測で問題になった場合、または高精度なレート制限が必要になった場合。

### 14.4 ゲスト認証

OAuthを必須にせず、署名付きHttpOnly CookieでゲストIDを発行する。

### 14.5 無料枠超過の防止

- ゲーム開始: 1 IPあたり1分10回まで
- 単語送信: 1セッションあたり毎秒3回まで
- ランキングAPIに短時間キャッシュを設定

### 14.6 READMEでの説明

```text
公開デモは、運用コストとコールドスタートを抑えるため、
Next.js Route HandlersとNeon PostgreSQLを使用した統合構成です。
ゲームロジックはUIから分離しており、利用者増加時にFastAPIへ
切り出せる構造にしています。Redisは計測上必要になるまで
導入せず、MVPではPostgreSQLのトランザクションと一意制約で
整合性を確保しています。
```

この説明により、「技術を多く採用した」のではなく「規模と運用コストを踏まえて構成を決定した」ことを示す。

---

## 15. 受け入れ条件（MVP完成の定義）

- GitHubのmainブランチ更新でVercelへ自動デプロイできる
- ゲストがログインなしでデイリーチャレンジを完走できる
- スコアと履歴がNeonへ保存される
- デイリー条件が全ユーザーで一致する
- サーバー側で単語・時間・得点を判定する
- 同じ入力リクエストを再送しても二重加点されない
- READMEにデモURL・構成理由・スコープを絞った理由を記載する
- 自動テストがCIで実行される（単語判定・ミッション判定・スコア計算の主要ロジックのみで可）

---

## 16. スコープ外・将来候補（実装しないが検討はした、として残す）

- スコアアタックモード、ステージモード（段階学習）
- コンボ倍率
- ゴースト比較・友達比較
- 取り消し機能
- 追加ミッション種別（開始文字系・速度系・順序系・高難度系）
- ミッション報酬のバリエーション（時間延長以外）
- パーセンタイル・中央値・代表ルート表示
- リプレイ閲覧API
- ユーザーによる単語投稿
- FastAPI分離、Redis導入
- 独自ドメイン

---

## 17. 開発の目安スケジュール（1〜2週間・副業と並行）

| 日程目安 | 内容 |
|---|---|
| 1〜2日目 | Next.js/Prisma/Neonのセットアップ、辞書スキーマとSeed投入 |
| 3〜4日目 | 単語正規化・接続判定・使用済み判定のロジックとテスト |
| 5〜6日目 | ミッション判定エンジン（4種類）とスコア計算 |
| 7〜8日目 | ゲーム画面UI、ゲストCookie認証 |
| 9〜10日目 | デイリーチャレンジ生成、ランキングAPI |
| 11〜12日目 | CI/CD、Sentry連携、レート制限、READMEの構成理由の言語化 |
| 13〜14日目 | バッファ・E2Eテスト・公開 |

厳密に守る必要はないが、「2週間でここまで」という区切りを最初に決めておくことで、途中で機能を足したくなったときに第16章へ退避する判断がしやすくなる。
