# ミッションしりとり 追加機能仕様書 v4（拡張版）

- 文書種別: 拡張機能仕様書
- 前提: v3（MVPスコープ版）が完成・公開済みであること
- 想定用途: 副業応募後、または継続的なポートフォリオ更新の材料
- 方針: v3で「将来候補」に退避した項目を、実装可能な粒度まで具体化する

---

## 0. 位置づけ

v3はスコープを絞って完成させることを優先した。本書はその後の拡張フェーズの仕様書であり、v3の構成（Next.js + Neon PostgreSQL、無料枠運用）を壊さずに追加できるものと、インフラ変更を伴うものを分けて記載する。

全機能を一度に実装する必要はない。第16章の優先順位に従って、1機能ずつ「動く状態」を保ったまま追加していくことを推奨する。これ自体もポートフォリオ上「段階的にリリースできる設計をしていた」という説明材料になる。

---

## 1. 優先順位の考え方

機能追加の判断基準は以下の3点。

1. **見せられる技術が増えるか**（単にゲームの厚みが増すだけの機能は後回し）
2. **v3の設計を壊さずに追加できるか**（バージョニングを最初から入れておいたのはこのため）
3. **応募先の求める技術と一致するか**（例: リアルタイム通信を求める企業への応募が増えたらWebSocket系を優先する、という調整も可）

第16章に推奨順序を示すが、応募状況に応じて入れ替えてよい。

---

## 2. モード追加：スコアアタック

### 2.1 内容

デイリーチャレンジと異なり、毎回ランダムな開始単語・ミッションで挑戦できるモード。いつでも何度でも遊べる。

### 2.2 変更点

- `game_sessions.game_mode` に `score_attack` を追加
- デイリー生成ロジック（第11章）からシード生成部分を切り離し、`game_mode` によって「固定シード」か「都度ランダム」かを切り替えるように共通化する
- ランキングテーブルにモード別の集計が必要になる（`rankings` を `game_mode` でフィルタする形にするか、別テーブルに分けるかは実装時に判断）

### 2.3 注意点

- デイリーとスコアアタックでランキングを混在させない（既存のインデックス `game_sessions_daily_score_idx` はデイリー専用のまま維持し、スコアアタック用に別インデックスを追加する）
- 無料枠のIPレート制限（v3 14.5）は、都度挑戦できるスコアアタックのほうが濫用されやすいため、モード別に制限値を分ける

### 2.4 見せられる技術

- 既存ロジックの共通化・抽象化の判断力
- モード別のクエリ設計

---

## 3. モード追加：ステージモード

### 3.1 内容

ルールを段階的に学習する一人用モード。固定のミッションセットを順にクリアしていく。

例:

1. 5語つなぐ
2. 5文字以上の単語を使う
3. 動物カテゴリを2回使う
4. 異なる末尾文字を5種類通る
5. 複数ミッションを同時達成する

### 3.2 変更点

- `stages` テーブルを新設（stage_number, mission_ids, unlock_condition）
- ユーザーの進捗を保存する `user_stage_progress` テーブルが必要（ゲストCookieでも一意に紐付けられるようにする）
- チュートリアル要素が強いため、初回訪問時の導線（オンボーディング）UIの追加も伴う

### 3.3 注意点

- ゲストの進捗保存は、Cookie失効時にリセットされる前提でよいか、アカウント機能（第11章参照・v3では未実装）と組み合わせるかを先に決める
- ステージモード単体はランキング要素を持たないため、無料枠への負荷は小さい

### 3.4 見せられる技術

- 進捗管理を伴うステートフルな設計
- オンボーディングUI設計

---

## 4. スコアシステム拡張：コンボ倍率

### 4.1 内容

連続成功回数に応じてスコア倍率を上げる。

```text
1〜4連続: x1.0
5〜9連続: x1.1
10〜14連続: x1.2
15連続以上: x1.3
```

無効入力でコンボをリセットする。

### 4.2 変更点

- `moveScore` の計算式にコンボ倍率の項を追加
- `scoring_version` を上げる（v3で設計時から保存しているため、ここで効いてくる）
- 過去のランキングは旧バージョンのまま保持し、新バージョンとは別集計にする

### 4.3 注意点

これはv3で最初から `scoring_version` を保存する設計にしておいた理由そのものである。実装時に「なぜバージョンを分けて保存していたか」を説明できると、設計の一貫性を示せる。

### 4.4 見せられる技術

- スコア計算式のバージョン管理・後方互換性の設計

---

## 5. 非同期比較：ゴースト機能

### 5.1 内容

リアルタイム対戦を実装せずに対戦感を出す。プレイ開始前または終了後に、以下のいずれかの基準で1件の過去プレイを選び「対戦相手」として比較表示する。

- 自分と近い過去スコア
- ランダム
- 前回の自分

### 5.2 プレイ中の表示

相手の単語そのものは表示せず、以下のみ表示する。

- 相手の最終スコア
- 現在の得点差
- 相手のミッション達成数

### 5.3 プレイ終了後の比較

```text
相手: りんご → ごりら → らっぱ → ぱせり → りす
自分: りんご → ごま → まくら → らくだ → だるま
```

比較項目: 得点差、単語数、ミッション達成順、通過した末尾文字、ミス数

### 5.4 変更点

- ゴースト選定用のクエリ（近いスコアのセッションを1件取得）
- プレイ中に相手の進捗をポーリングまたは事前スナップショットとして渡す方式を選ぶ（WebSocketは使わない方針を維持）
- `game_moves` のログがすでに保存されているため（v3で設計済み）、ルート比較の実装自体は追加コストが小さい

### 5.5 注意点

ゴースト選定のクエリは負荷になりやすいため、無料枠のDB接続数・実行時間を計測しながら導入する。必要ならv3 14.3の「Redis導入条件」に該当する（上位スコアのキャッシュ）。

### 5.6 見せられる技術

- 非同期処理設計、ログを活用した機能拡張の設計力

---

## 6. 補助機能拡張：取り消し機能

### 6.1 内容

直前の1手を取り消す。1プレイ1回まで。取り消した単語で獲得した得点と進捗を巻き戻す。

### 6.2 変更点

- 取り消し時にサーバー側で当該 `game_moves` レコードを無効化し、`game_sessions` の現在スコア・ミッション進捗を再計算する
- 「イベントを再計算する」処理を独立した関数にしておくと、後述のリプレイ機能とも共有できる

### 6.3 注意点

取り消し後の二重送信・不正操作を防ぐため、取り消しリクエストにも冪等性キーを持たせる（v3で単語送信APIに実装済みの冪等性の仕組みを流用できる）。

### 6.4 見せられる技術

- イベントソーシング的な考え方（状態を都度再計算する設計）

---

## 7. ミッション種別の拡張

v3では4種類（単語数系・文字数系・カテゴリ系・末尾文字系）のみ実装した。以下を追加する。

### 7.1 開始文字系

- 濁音から始まる単語を2回使う
- ら行から始まる単語を2回使う
- 異なる開始文字を8種類使う

### 7.2 速度系

- 10秒以内に3語答える
- 平均回答時間を5秒以内にする

サーバー側で各 `game_moves` にタイムスタンプを保存済み（v3設計）のため、判定ロジックの追加のみで対応できる。

### 7.3 連続・順序系

- 動物→食べ物の順で使う
- 短い単語→長い単語の順で使う

判定には「直近N手の履歴」を参照する必要があり、既存の単発判定（現在の単語だけを見る）とは判定関数のシグネチャが変わる点に注意する。

### 7.4 制約系

- ヒントを使わず10語つなぐ
- ミスを一度もせずにクリアする

### 7.5 高難度系

- 1単語で2枚以上を同時達成する
- 残り5秒以内にミッションを達成する

### 7.6 実装上の注意

- ミッション種別が増えるほど、第8.4章（v3）の「組み合わせ制御」が複雑になる。新種別を追加するたびに、既存ミッションとの相性（タグの衝突）を再確認する
- 種別ごとに判定関数を追加していく設計にしておくと、テストも種別単位で追加できる

---

## 8. ミッション報酬のバリエーション

v3ではスコア加算のみだった報酬を拡張する。

- 制限時間を3秒延長
- 次の単語の得点を1.5倍
- ミスによる時間ペナルティを1回無効
- カードを1枚交換
- ヒント回数を1回回復

### 8.1 変更点

`missions.reward` の `type` を増やし、報酬適用ロジックに分岐を追加する。カード交換のみ、進行中のミッションセット自体を書き換える必要があるため実装コストが高い。優先度は最後でよい。

---

## 9. デイリーチャレンジ結果表示の拡張

v3では「順位」「自己ベストとの差」のみだった結果画面に、以下を追加する。

- パーセンタイル
- 全参加者平均・中央値
- ミッション達成率
- 最長連鎖
- 同じ条件の代表ルート（上位プレイのルートを1件表示）
- 前日との差

### 9.1 実装条件

これらは参加者数がある程度いないと意味を持たない（パーセンタイルは母数が少ないと不安定）。**目安として1日あたりのデイリー挑戦者が数十人を超えてから実装する**のが妥当。それまでは第16章の優先順位を下げてよい。

### 9.2 変更点

```sql
SELECT
    user_id,
    score,
    RANK() OVER (ORDER BY score DESC) AS rank,
    PERCENT_RANK() OVER (ORDER BY score DESC) AS percentile
FROM game_sessions
WHERE game_mode = 'daily' AND daily_date = CURRENT_DATE AND is_ranked = true;
```

集計クエリの負荷が上がるため、結果画面表示用に短時間キャッシュを検討する（v3 14.3のRedis導入条件に該当しうる）。

---

## 10. リプレイ閲覧API

### 10.1 内容

```text
GET /api/v1/game-sessions/:id/replay
```

保存済みの `game_moves` ログを時系列で返し、フロント側で「他人のプレイを1手ずつ再生する」UIを実装する。

### 10.2 注意点

- ログはv3から保存しているため、バックエンド側の追加実装は小さい
- 公開範囲（誰のリプレイでも見られるか、ランキング上位のみか）を決める。全件公開にすると辞書の全量が推測できてしまう可能性があるため、上位N件程度に絞るのが無難

### 10.3 見せられる技術

- ログの再生UI、既存データの再利用設計

---

## 11. ユーザーによる単語投稿機能

### 11.1 内容

プレイヤーが辞書にない単語を候補として申請できる機能。

- 申請語は即時公開しない
- 管理者承認後に辞書へ追加
- 申請者名は公開しない
- 同一語の重複申請を抑止
- 監査ログを保存

### 11.2 変更点

- `word_submissions` テーブル（申請語、申請者、状態、承認者、承認日時）
- 簡易な管理画面（承認・却下ボタン）が必要になる。この管理画面自体もポートフォリオ上「管理者向け機能を設計できる」証拠になる

### 11.3 注意点

不適切語の混入対策として、自動フィルタ（NGワードリスト）を通してから人力承認に回す2段階にする。この機能はモデレーションコストが発生し続けるため、実装優先度は低めでよい。個人開発として継続運用できるかを先に検討する。

---

## 12. インフラ拡張：FastAPI分離

### 12.1 移行条件（v3 14.3を踏襲）

以下のいずれかが発生したら検討する。

- Pythonの形態素解析・辞書生成処理をオンラインAPIとして提供したくなった
- Next.js Route Handlersの実行時間・メモリ制約が問題になった
- APIを複数クライアント（モバイルアプリ等）から利用する必要が出た
- Pythonバックエンドの実務能力を成果物として追加したい（本業・応募先の技術スタックがPythonの場合、これが最も現実的な動機になる）

### 12.2 移行順序

1. ドメインロジックのテストを固定する（先にテストがないと分離時に壊れたか分からない）
2. OpenAPI契約を定義する
3. 読み取り専用API（辞書検索など）から切り出す
4. 単語判定APIを移行する
5. ゲーム入力APIを移行する
6. 認証・Cookie連携を整理する
7. Next.js Route HandlersをBFF（Backend For Frontend）として残すか判断する

### 12.3 注意点

無料公開版の安定性を損なわないよう、別ブランチまたは段階的リリースで進める。分離すると環境変数管理・CORS設定・コールドスタート対策など、機能を増やさない運用コストが増える点はv3で明記した通り。動機が明確でないまま実施しない。

---

## 13. インフラ拡張：Redis導入

### 13.1 移行条件（v3 14.3を踏襲）

- ランキングAPIのDB負荷が実測で問題になった
- IP・ユーザー単位の高精度なレート制限が必要になった
- 非同期ジョブキューが必要になった（例: 大量の辞書再インデックス処理）

### 13.2 用途を限定する

- レート制限
- 上位ランキングのキャッシュ
- セッションの短期キャッシュ

PostgreSQLに保存すべき永続データをRedisだけに保存しない。追加時はUpstash Redisの無料枠を候補とする。

---

## 14. 独自ドメイン

ポートフォリオ全体を1つのドメインで整理したい場合、または応募書類に長期間掲載する場合に追加する。取得費用は運用費（0円方針）に含めない。優先度は最も低い。

---

## 15. バージョニング・互換性の注意点

拡張フェーズ全体を通じて守るべき原則。

- スコア計算式を変更したら `scoring_version` を必ず上げる
- 辞書を更新したら `dictionary_version` を必ず上げる
- ミッション判定ロジックを変更したら `mission_rule_version` を必ず上げる
- 旧バージョンのランキングは新バージョンと混在させない
- マイグレーションは既存データを壊さない方向（カラム追加）を優先し、破壊的変更（型変更・削除）は避けられる限り避ける

v3の段階でこれらのバージョンフィールドを最初から用意しておいたのは、まさにこの章のためである。

---

## 16. 推奨実装順序

実装コストと見せられる技術のバランスで並べた目安。応募先の技術要件に応じて入れ替えてよい。

| 優先度 | 機能 | 理由 |
|---|---|---|
| 高 | コンボ倍率（第4章） | バージョニング設計の実証として最も分かりやすい |
| 高 | ミッション種別の拡張（第7章、まず開始文字系・速度系） | ルールエンジンの拡張性を示せる、実装コストが小さい |
| 高 | 取り消し機能（第6章） | 状態再計算の設計力を示せる |
| 中 | スコアアタックモード（第2章） | 既存ロジックの共通化の練習になる |
| 中 | ゴースト機能（第5章） | 差別化要素として見た目のインパクトが大きい |
| 中 | リプレイ閲覧API（第10章） | 実装コストが小さく、ログ活用の実例になる |
| 低 | ステージモード（第3章） | UI実装コストが大きい割に技術的な見せ場が少ない |
| 低 | デイリー結果表示の拡張（第9章） | 参加者数がある程度必要、それまで意味が薄い |
| 低 | ミッション報酬のバリエーション（第8章） | カード交換の実装コストが高い |
| 要検討 | ユーザー単語投稿（第11章） | 継続的な運用コストが発生する |
| 要検討 | FastAPI分離（第12章） | 明確な動機がない限り着手しない |
| 要検討 | Redis導入（第13章） | 実測で負荷が確認できてから |
| 最低 | 独自ドメイン（第14章） | 機能ではなく体裁の話 |

1機能ごとに「動く状態でコミット・デプロイする」を徹底し、複数機能を同時並行で手を付けない。これはv3を1〜2週間で完成させた進め方と同じ原則である。
